server:
  port: 8001
  netty:
    connection-timeout: 2s
    idle-timeout: 30s

spring:
  application:
    name: vendor-service

  rabbitmq:
    host: fuji.lmq.cloudamqp.com
    port: 5672
    username: tdteuzte
    password: lIrc_ZdOdoQZ3haXShEgsl8EpOn2jnXG
    virtual-host: tdteuzte

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://keycloak.icuture.icu/realms/cloud/protocol/openid-connect/certs  

  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://ep-dawn-grass-a1wt8d9l-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_DzgSH9bytf0r
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    url: jdbc:postgresql://ep-dawn-grass-a1wt8d9l-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    user: neondb_owner
    password: npg_DzgSH9bytf0r
    locations: classpath:db/migration           
    baseline-on-migrate: true                    
    clean-disabled: false

logging:
  level:
    root: INFO
    brave: DEBUG
    reactor.netty: DEBUG
    org.springframework.web: DEBUG
    org.springframework.cloud: DEBUG
    com.cloud.vendor-service: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/vendor-service.log
    max-size: 10MB
    max-history: 7

management:
  health:
    show-details: always
    show-components: always
    diskspace:
      enabled: true
    refresh:
      enabled: true
    probes:
      enabled: true
    
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers,env,threaddump,heapdump,prometheus,zipkin
      base-path: /actuator
    
  mappings:
    health: healthcheck

  tracing:
    sampling:
      probability: 1.0
  
  # zipkin:
  #   tracing:
  #     endpoint: http://zipkin:9411/api/v2/spans
  #     connect-timeout: 1s
  #     read-timeout: 5s
